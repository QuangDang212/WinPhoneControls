<UserControl
    x:Class="WinPhoneControls.Camera.CameraCaptureControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{Binding RelativeSource={RelativeSource Self}}"
    mc:Ignorable="d">
    
    <Grid>
        <Grid x:Name="CameraCaptureGrid">
            <CaptureElement x:Name="PreviewElement" Margin="-64,0,0,0" Stretch="UniformToFill" />
            
            <Grid x:Name="SettingsGrid" VerticalAlignment="Center" HorizontalAlignment="Left">
                
            </Grid>
            
            <Grid x:Name="CameraButtonGrid" VerticalAlignment="Center" HorizontalAlignment="Right">
                <AppBarButton x:Name="CameraButton" Icon="Camera" RenderTransformOrigin="0.5, 0.5" />
            </Grid>
        </Grid>
    </Grid>
</UserControl>
